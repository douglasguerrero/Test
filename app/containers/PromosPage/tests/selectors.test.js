// import { fromJS } from 'immutable';
// import { makeSelectPromosPageDomain } from '../selectors';

// const selector = makeSelectPromosPageDomain();

describe('makeSelectPromosPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
